<resources>

    <style name="MaterialTheme" parent="ParentMaterialTheme" />
    <!-- Customize your theme here. -->
    <style name="ParentMaterialTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Parent theme for all Pre-Lolipop and Lollipop above versions 1.o For all device. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>

    <item name="colorControlHighlight">@color/colorHighlignt</item>
    <item name="colorButtonNormal">@color/colorButtonNormal</item>
    <item name="colorControlActivated">@color/coclorControlAcitivate</item>
</style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light"><!--Text color of Toolbar Title-->
        <item name="android:textColorPrimary">@android:color/white</item> <!--color of overflow(menu) and color od navigation Drawer-->
        <item name="android:textColorSecondary">@android:color/white</item>

    </style>

    <style name="PopupTheme" parent="ThemeOverlay.AppCompat.Light" >

        <item name="android:textColorPrimary">#9E9E9E</item> <!--Text color of overflow Menu -->
        <item name="android:background">#fffcfff6</item><!-- Background of overflow menu -->
        </style>
</resources>
